Object.create() 
方法创建一个拥有指定原型和若干个指定属性的对象。
Object.create(proto, [ propertiesObject ])
------------------------------------------------
in
判断 prop 是否在对象 objectName 中
objectName 可以是 Arrays object json 
prop in objectName
------------------------------------------------
!!
确保返回值是 Boolean 类型而已, 取了两次 非